#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    bool circularArrayLoop(vector<int>& nums) {
        int len = nums.size();
        if(len <= 0) return false;

        std::vector<bool> flags(nums.size(), false);
        
        

        return false;
    }
};

int main() {
    return 0;
}